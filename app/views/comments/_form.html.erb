<%= form_with model: [commentable, Comment.new], local: true do |form| %>
    <div class="field">
      <%= form.text_area :content, rows: 4, placeholder: 'Write a comment...' %>
    </div>
    <div class="actions">
      <%= form.submit 'Post Comment' %>
    </div>
<% end %>
  